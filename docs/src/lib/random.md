# Random Numbers

```@meta
CurrentModule = cuTile
```

In-kernel `rand`, `randn` and `randexp` are `Base`/`Random` functions that cuTile overlays;
their usage is documented in the [Random Numbers](../man/random.md) manual page. The entries
below are the cuTile-specific stream types.

## Streams

```@docs
DeviceRNG
RNG
```
